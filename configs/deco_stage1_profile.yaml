# Stage 1 profiling config derived from DeCo_XL.yaml.
# It keeps the official model architecture and checkpoint compatibility, but
# swaps only the sampler class for a project-local profiling wrapper.
seed_everything: true
tags:
  exp: &exp DeCo_256_XL_stage1_profile
torch_hub_dir: ~/.cache/torch/hub
huggingface_cache_dir: null
trainer:
  default_root_dir: ./stage1_deco_profile
  accelerator: auto
  strategy: auto
  devices: auto
  num_nodes: 1
  precision: bf16-mixed
  logger: null
  num_sanity_val_steps: 0
  max_steps: 4000000
  val_check_interval: 2000000
  check_val_every_n_epoch: null
  log_every_n_steps: 50
  deterministic: null
  inference_mode: true
  use_distributed_sampler: false
  callbacks:
    - class_path: src.callbacks.model_checkpoint.CheckpointHook
      init_args:
        every_n_train_steps: 200000
        save_top_k: -1
        save_last: true
    - class_path: src.callbacks.save_images.SaveImagesHook
      init_args:
        save_dir: stage1_profile_ode10_cfg3.2_interval0.1_seed0
        save_compressed: true
  plugins:
    - src.plugins.bd_env.BDEnvironment
model:
  vae:
    class_path: src.models.autoencoder.pixel.PixelAE
    init_args:
      scale: 1.0
  denoiser:
    class_path: src.models.transformer.dit_c2i_DeCo.PixNerDiT
    init_args:
      in_channels: 3
      patch_size: 16
      num_groups: 16
      hidden_size: &hidden_dim 1152
      hidden_size_x: 32
      num_blocks: 31
      num_cond_blocks: 28
      nerf_mlpratio: 2
      num_classes: &num_classes 1000
  conditioner:
    class_path: src.models.conditioner.class_label.LabelConditioner
    init_args:
      num_classes: *num_classes
  diffusion_trainer:
    class_path: src.diffusion.flow_matching.training_repa_DeCo.REPATrainer
    init_args:
      lognorm_t: true
      encoder:
        class_path: src.models.encoder.DINOv2
        init_args:
          weight_path: /root/.cache/torch/hub/checkpoints/dinov2_vitb14_pretrain.pth
      align_layer: 8
      proj_denoiser_dim: *hidden_dim
      proj_hidden_dim: *hidden_dim
      proj_encoder_dim: 768
      null_condition_p: 0.2
      scheduler: &scheduler src.diffusion.flow_matching.scheduling.LinearScheduler
  diffusion_sampler:
    class_path: pfc.profiling.deco_profiled_sampler.ProfiledEulerSampler
    init_args:
      num_steps: 10
      guidance: 3.2
      guidance_interval_min: 0.1
      guidance_interval_max: 1.0
      scheduler: *scheduler
      w_scheduler: src.diffusion.flow_matching.scheduling.LinearScheduler
      guidance_fn: src.diffusion.base.guidance.simple_guidance_fn
      step_fn: src.diffusion.flow_matching.sampling.ode_step_fn
  ema_tracker:
    class_path: src.callbacks.simple_ema.SimpleEMA
    init_args:
      decay: 0.9999
  optimizer:
    class_path: torch.optim.AdamW
    init_args:
      lr: 1e-4
      weight_decay: 0.0
data:
  train_dataset:
    class_path: src.data.dataset.imagenet.PixImageNet
    init_args:
      root: /mnt/iset/nfs-main/public/datasets/ILSVRC/Data/CLS-LOC/train
      resolution: 256
  eval_dataset:
    class_path: src.data.dataset.randn.ClassLabelRandomNDataset
    init_args:
      num_classes: 1000
      conditions: &profile_conditions
        - 0
        - 1
        - 2
        - 3
      max_num_instances: 4
      latent_shape:
        - 3
        - 256
        - 256
  pred_dataset:
    class_path: src.data.dataset.randn.ClassLabelRandomNDataset
    init_args:
      num_classes: *num_classes
      conditions: *profile_conditions
      max_num_instances: 4
      latent_shape:
        - 3
        - 256
        - 256
  train_batch_size: 32
  train_num_workers: 4
  pred_batch_size: 4
  pred_num_workers: 1

